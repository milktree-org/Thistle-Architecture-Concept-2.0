/**
 * The example feasibility, and where it lives.
 *
 * Ed sent the Claremont Road pack on 23 September 2026, which is the redacted
 * example the package page had been promising since August. Four documents
 * merged into one: the overview, the planning research, the space standards and
 * the sketch. Luton Borough Council's own HMO standards document came with them
 * and is deliberately NOT republished here, because it is the council's
 * copyright; the page links to it on luton.gov.uk instead.
 *
 * The file name carries a random suffix. That is not security, since anyone
 * given the link can pass it on. It only stops the address being guessed from
 * the page, so the email gate is worth something.
 *
 * Named here rather than typed in three places: the card reveals it, the
 * Formspree autoresponse emails it, and a rename that missed one would send
 * people to a 404 they cannot report.
 */
export const SAMPLE_REPORT_PATH = '/downloads/thistle-example-feasibility-claremont-road-9e72bfd0.pdf';
export const SAMPLE_REPORT_URL = 'https://www.thistlearchitecture.co.uk' + SAMPLE_REPORT_PATH;

/** Luton's HMO standards, cited in the pack. Linked, never rehosted. */
export const SAMPLE_REPORT_COUNCIL_DOC =
  'https://www.luton.gov.uk/Housing/Pages/Private-sector-housing.aspx';
